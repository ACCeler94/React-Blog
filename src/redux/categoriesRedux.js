// selectors
export const getAllCategories = ({ categories }) => categories;


//reducer
export const categoriesReducer = (statePart = [], action) => {
  switch (action.type) {
    case '':
      return statePart
    default:
      return statePart
  }
}